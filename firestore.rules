rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny-all by default. Add per-collection rules as the app grows.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
