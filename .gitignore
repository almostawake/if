node_modules/
client/node_modules/
client/build/
client/.svelte-kit/
client/static/*.json

# Firebase
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# Env
.env
.env.local
.env.*.local
# Deployment-specific Firebase Web config — written per-project at deploy
# time, not template-generic.
client/.env.production
client/.env

# Service-account keys (if we ever cache them locally during deploy)
.if/sa-keys/

# OS
.DS_Store
