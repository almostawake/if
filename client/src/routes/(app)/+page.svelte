<!--
  Home page (/). Currently a placeholder; the app's first real feature
  will land here. Lives inside (app) so it inherits the auth gate, the
  top menu, and the page chrome (via <Page>) for free.
-->
<script lang="ts">
  import Page from '$lib/components/Page.svelte';
</script>

<Page title="home" description="ah, one day a dashboard here?" />
